pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
        maven { url 'https://jitpack.io' } // Required for MPAndroidChart
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS) // <-- Changed to allow central control
    repositories {
        google()
        mavenCentral()
        maven { url 'https://jitpack.io' } // Add this to resolve MPAndroidChart
    }
}

rootProject.name = "GoalGuru" // or "Calculator" if that’s your real app name
include ':app'
